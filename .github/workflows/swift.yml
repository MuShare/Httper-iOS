name: Swift

on: [push]

jobs:
  build:

    runs-on: macOS-latest

    steps:
    - uses: CultureHQ/actions-bundler@v1.0.0
    - name: Bundle install
      run: bundle install
    - name: Run test
      run: bundle exec fastlane test
